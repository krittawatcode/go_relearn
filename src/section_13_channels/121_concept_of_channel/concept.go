package main

func main() {

	/*
		ch := make(chan <type>)
		ex. ch := make(chan int)
	*/

	/*
		channel is a 'reference type' !!
		it's mean channel will point to same ref even when we pass channal to other method by value
		- when we receive value back (send value to ch in method)
		  value will send to ref channal before pass value)
		- like in part 120 we copy value from channel to method 'training'
		  and pass it to ch
	*/

	/*
		we can compare channel
	*/

	/*
		Two type of channel
		1. unbuffered
		2. buffered
	*/
}
